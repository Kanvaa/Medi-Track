services:
  - type: web
    name: medi-track
    env: docker
    plan: free
    envVars:
      - key: SPRING_PROFILES_ACTIVE
        value: default
      - key: APP_ENCRYPTION_SECRET
        generateValue: true
